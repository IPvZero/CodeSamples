---

inventory:
    plugin: SimpleInventory
    options:
            #CHANGE THESE PATHS TO MATCH YOUR OWN
        host_file: "hosts.yaml"
        group_file: "groups.yaml"
        defaults_file: "defaults.yaml"

runners:
    plugin: threaded
    options:
        num_workers: 25
